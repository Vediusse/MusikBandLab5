package exceptions;

public class IllegalArgumentForCordinateXException extends IllegalArgumentException {
    public IllegalArgumentForCordinateXException(String message) {
        super(message);
    }
}
